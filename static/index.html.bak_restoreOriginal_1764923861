<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Dual AI Console — Cem Edition</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: #0b1220;
      color: #e5e7eb;
      height: 100vh;
      display: flex;
      flex-direction: column;
    }
    header {
      padding: 10px 16px;
      border-bottom: 1px solid #1f2937;
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: linear-gradient(to right, #0b1220, #111827);
    }
    header h1 {
      font-size: 16px;
      margin: 0;
    }
    header span {
      font-size: 12px;
      color: #9ca3af;
    }
    main {
      flex: 1;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 0;
      overflow: hidden;
    }
    .panel {
      display: flex;
      flex-direction: column;
      border-left: 1px solid #1f2937;
    }
    .panel:first-child {
      border-left: none;
      border-right: 1px solid #1f2937;
    }
    .panel-header {
      padding: 8px 12px;
      border-bottom: 1px solid #1f2937;
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: #020617;
    }
    .panel-header h2 {
      font-size: 14px;
      margin: 0;
      display: flex;
      align-items: center;
      gap: 6px;
    }
    .badge {
      font-size: 10px;
      padding: 2px 6px;
      border-radius: 999px;
      background: #111827;
      color: #9ca3af;
    }
    .body {
      flex: 1;
      display: flex;
      flex-direction: column;
      padding: 8px;
      gap: 8px;
      overflow: hidden;
    }
    textarea {
      width: 100%;
      resize: none;
      min-height: 80px;
      border-radius: 8px;
      border: 1px solid #374151;
      background: #020617;
      color: #e5e7eb;
      padding: 8px;
      font-size: 13px;
      font-family: inherit;
    }
    textarea:focus {
      outline: 1px solid #f97316;
      border-color: #f97316;
    }
    .output {
      flex: 1;
      border-radius: 8px;
      border: 1px solid #1f2937;
      background: #020617;
      padding: 8px;
      font-size: 13px;
      overflow-y: auto;
      white-space: pre-wrap;
    }
    .controls {
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 11px;
    }
    button {
      border-radius: 999px;
      border: none;
      padding: 6px 12px;
      font-size: 12px;
      cursor: pointer;
      background: #f97316;
      color: #111827;
      font-weight: 500;
    }
    button:disabled {
      opacity: 0.6;
      cursor: default;
    }
    select {
      background: #020617;
      color: #e5e7eb;
      border-radius: 999px;
      border: 1px solid #374151;
      padding: 4px 8px;
      font-size: 11px;
    }
    input[type="file"] {
      font-size: 10px;
      color: #9ca3af;
    }
    .usage {
      color: #6b7280;
      font-size: 11px;
    }
    footer {
      padding: 4px 10px;
      font-size: 10px;
      color: #6b7280;
      border-top: 1px solid #1f2937;
      background: #020617;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
  </style>
<!-- WHITE THEME PATCH START -->
<style>
  body { background:#ffffff !important; color:#111111 !important; }
  .chat-column, .chat-panel, .chat-box {
    background:#ffffff !important; border:1px solid #e0e0e0 !important;
    border-radius:10px !important;
  }
  textarea, input[type=text], input[type=search] {
    background:#fafafa !important; color:#111 !important;
    border:1px solid #d0d0d0 !important; border-radius:8px !important;
  }
  header, nav, .navbar {
    background:#ffffff !important; border-bottom:1px solid #e5e5e5 !important;
  }
</style>
<!-- WHITE THEME PATCH END -->

<!-- WHITE THEME V2 PATCH -->
<style>
  body {
    background: #ffffff !important;
    color: #111111 !important;
    font-size: 16px;
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif;
  }

  /* Ana iki panel + input/output alanları */
  #openai-output, #anthropic-output,
  #openai-input, #anthropic-input,
  .chat-panel, .chat-column, .chat-box,
  .output-box, .input-box {
    background: #ffffff !important;
    color: #111111 !important;
    border-radius: 10px !important;
    border: 1px solid #e0e0e0 !important;
  }

  /* Textarea, pre, kod blokları */
  textarea, pre, code {
    font-size: 16px !important;
    line-height: 1.5;
    background: #fafafa !important;
    color: #111111 !important;
    border-radius: 8px !important;
    border: 1px solid #d0d0d0 !important;
  }

  /* Butonlar daha okunaklı dursun */
  button {
    font-size: 14px;
  }
</style>
<!-- /WHITE THEME V2 PATCH -->


<!-- WHITE THEME V3 PATCH -->
<style>
  /* Global font & colors */
  html, body {
    background: #ffffff !important;
    color: #111111 !important;
    font-size: 18px !important;
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif;
  }

  /* Bütün layout ve iki paneli zorla beyaz yap */
  .app-root, .main-layout, .dual-console, .chat-layout,
  .chat-columns, .chat-column, .chat-panel,
  .left-pane, .right-pane, .column-left, .column-right,
  #openai-panel, #anthropic-panel,
  #openai-output, #anthropic-output {
    background: #ffffff !important;
    color: #111111 !important;
    border-color: #e0e0e0 !important;
  }

  /* Mesaj kutuları, textarea vs. */
  textarea, pre, code, .message, .chat-message, .output-box, .input-box {
    font-size: 17px !important;
    line-height: 1.55;
    background: #fafafa !important;
    color: #111111 !important;
    border-radius: 8px !important;
    border: 1px solid #d0d0d0 !important;
  }

  button {
    font-size: 15px !important;
  }
</style>
<!-- /WHITE THEME V3 PATCH -->


<!-- WHITE THEME V4 PATCH – Nuclear Mode -->
<style>
  /* Global */
  html, body {
    background: #ffffff !important;
    color: #111111 !important;
    font-size: 18px !important;
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, sans-serif;
  }

  /* All wrappers, left + right panels */
  .container, .wrapper, .layout, .main-layout,
  .dual-console, .chat-container,
  .chat-columns, .chat-column,
  .left-panel, .right-panel,
  .column-left, .column-right,
  .left-pane, .right-pane,
  #openai-panel, #anthropic-panel,
  #openai-output, #anthropic-output {
      background: #ffffff !important;
      color: #111111 !important;
      border-color: #d0d0d0 !important;
  }

  /* Anything that looks like a chat box */
  textarea, pre, code, .message, .chat-message,
  .output-box, .input-box, .chat-output,
  .result-window, .response-box {
      background: #fafafa !important;
      color: #111 !important;
      font-size: 17px !important;
      line-height: 1.55;
      border: 1px solid #d0d0d0 !important;
      border-radius: 8px !important;
  }

  /* Catch-all: any div with dark background */
  div[style*="background:#0"], div[style*="background: #0"],
  div[style*="background-color:#0"], div[style*="background-color: #0"],
  div[style*="background:#1"], div[style*="background: #1"] {
      background: #ffffff !important;
      color: #111111 !important;
  }

</style>
<!-- END WHITE THEME V4 PATCH -->


<!-- WHITE THEME V5 – ULTRA OVERRIDE -->
<style>
  /* SAĞ PANEL İÇİN EVRENSEL BEYAZ TEMA */
  .right-panel, .right-pane, .column-right, #claude-panel, #anthropic-panel,
  .claude-container, .claude-column, .claude-side,
  [class*="right"], [id*="right"], [class*="anthropic"], [id*="anthropic"] {
      background: #ffffff !important;
      color: #111 !important;
  }

  /* SAĞ CHAT OUTPUT KUTULARI */
  #anthropic-output, #claude-output,
  .right-output, .claude-output, .output-box {
      background: #fafafa !important;
      color: #111 !important;
      font-size: 18px !important;
      border: 1px solid #ccc !important;
      border-radius: 8px !important;
      padding: 12px !important;
  }

  /* SAĞ PANELDE BULUNAN TÜM DIVLERİ BEYAZLA */
  #anthropic-panel div, #claude-panel div,
  .right-panel div, .column-right div {
      background-color: #ffffff !important;
      color: #111 !important;
  }

  /* Koyu arka plan içeren her div’i yakala */
  div[style*="rgb(0"], div[style*="#0"], div[style*="#1"] {
      background: #ffffff !important;
      color: #111 !important;
  }
</style>
<!-- END WHITE THEME V5 -->


<!-- TYPOGRAPHY PATCH V1 -->
<style>
  /* Global font (Apple tarzı, Türkçe karakter tam destek) */
  html, body {
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", system-ui, -system-ui, sans-serif;
    font-size: 18px !important;
    line-height: 1.6;
    color: #111111;
  }

  /* Üstteki prompt input alanları */
  textarea,
  .prompt-input,
  .chat-input,
  .input-box {
    font-size: 18px !important;
    line-height: 1.6 !important;
  }

  /* Cevap alanları (output) */
  #openai-output,
  #anthropic-output,
  .chat-output,
  .response-box,
  .output-box,
  pre,
  code {
    font-size: 18px !important;
    line-height: 1.7 !important;
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", system-ui, -system-ui, sans-serif;
  }

  /* Butonlar ve küçük metinler */
  button,
  .model-select,
  .token-info,
  .header-text,
  .badge {
    font-size: 15px !important;
  }

  /* Placeholder’lar da okunabilir olsun */
  textarea::placeholder,
  .prompt-input::placeholder {
    font-size: 16px;
    color: #777777;
  }
</style>
<!-- /TYPOGRAPHY PATCH V1 -->


<!-- TYPOGRAPHY PATCH V3 — ULTRA READABLE -->
<style>
  /* GLOBAL */
  html, body {
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", system-ui, sans-serif !important;
    font-size: 22px !important;     /* 🔥 TEMEL BOYUT ARTTI */
    line-height: 1.75 !important;   /* Daha ferah */
    color: #111 !important;
  }

  /* INPUT + PROMPT ALANLARI */
  textarea,
  input,
  .prompt-input {
    font-size: 22px !important;      /* 🔥 2 kat daha büyük */
    line-height: 1.7 !important;
    padding: 16px !important;
  }

  textarea::placeholder,
  .prompt-input::placeholder {
    font-size: 20px !important;
    color: #777 !important;
  }

  /* CEVAP ALANLARI (OUTPUT) */
  #openai-output,
  #anthropic-output,
  .chat-output,
  .response-box,
  .output-box,
  pre,
  code {
    font-size: 24px !important;     /* 🔥 Cevap metni büyük */
    line-height: 1.8 !important;
    white-space: pre-wrap !important;
    word-break: break-word !important;
    color: #111 !important;
  }

  /* BUTONLAR + SEÇİCİLER */
  button,
  .model-select,
  .token-info,
  .badge {
    font-size: 18px !important;
    padding: 10px 16px !important;
  }
</style>
<!-- /TYPOGRAPHY PATCH V3 -->


<!-- TYPOGRAPHY PATCH V4 — MAX READABLE UI -->
<style>
  /* Global base – HER ŞEYİN TEMELİ */
  html, body {
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", system-ui, sans-serif !important;
    font-size: 26px !important;       /* 🔥 temel boyut büyütüldü */
    line-height: 1.8 !important;
    color: #111111 !important;
  }

  /* Üst bar, menüler, nav */
  header, nav, nav * ,
  .top-bar, .top-bar * {
    font-size: 24px !important;
  }

  /* Başlıklar */
  h1 { font-size: 32px !important; }
  h2 { font-size: 30px !important; }
  h3 { font-size: 28px !important; }

  /* Prompt input alanları */
  textarea,
  input,
  .prompt-input,
  .chat-input {
    font-size: 28px !important;
    line-height: 1.8 !important;
    padding: 18px !important;
  }

  textarea::placeholder,
  .prompt-input::placeholder {
    font-size: 24px !important;
    color: #777 !important;
  }

  /* ÇIKTI / CEVAP ALANLARI */
  #openai-output,
  #anthropic-output,
  .chat-output,
  .response-box,
  .output-box,
  pre,
  code {
    font-size: 28px !important;       /* 🔥 asıl okuduğun yer */
    line-height: 1.9 !important;
    white-space: pre-wrap !important;
    word-break: break-word !important;
    color: #111 !important;
  }

  /* Butonlar, model seçici, token info vs. */
  button,
  .model-select,
  select,
  .token-info,
  .badge,
  .pill {
    font-size: 22px !important;
    padding: 10px 18px !important;
  }
</style>
<!-- /TYPOGRAPHY PATCH V4 -->


<!-- GLOBAL FONT OVERRIDE — ChatGPT placeholder style x2 -->
<style>
  /* Ana font — ChatGPT placeholder fontunun birebir aynısı */
  :root {
    --base-font: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", Helvetica, Arial, sans-serif;
  }

  /* Tüm UI’nin temel fontu — x2 büyütme */
  html, body {
    font-family: var(--base-font) !important;
    font-size: 32px !important;     /* 🔥 placeholder’ın TAM 2X versiyonu */
    line-height: 1.9 !important;
    color: #111 !important;
  }

  /* Başlıklar */
  h1 { font-size: 40px !important; font-weight: 600 !important; }
  h2 { font-size: 36px !important; font-weight: 600 !important; }
  h3 { font-size: 32px !important; font-weight: 500 !important; }

  /* Model seçici, nav, üst menü */
  nav, nav *, header, header * {
    font-size: 28px !important;
    font-weight: 500 !important;
  }

  /* Textarea: placeholder stili x2 */
  textarea, .prompt-input {
    font-size: 32px !important;
    font-family: var(--base-font) !important;
    padding: 20px !important;
    line-height: 1.9 !important;
  }

  textarea::placeholder {
    font-size: 28px !important;
    color: #777 !important;
    font-family: var(--base-font) !important;
  }

  /* Cevap alanları */
  #openai-output,
  #anthropic-output,
  .response-box,
  .output-box,
  pre, code {
    font-size: 32px !important;     /* 🔥 ana okuma alanı */
    font-family: var(--base-font) !important;
    line-height: 1.9 !important;
    white-space: pre-wrap !important;
  }

  /* Butonlar */
  button {
    font-size: 26px !important;
    font-family: var(--base-font) !important;
    padding: 12px 22px !important;
  }
</style>
<!-- /GLOBAL FONT OVERRIDE -->


<!-- GLOBAL SUPER FONT OVERRIDE — x2 Bigger Again -->
<style>
  :root {
    --base-font: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", Helvetica, Arial, sans-serif;
  }

  /* Ana body — tekrar 2× büyütme */
  html, body {
    font-family: var(--base-font) !important;
    font-size: 52px !important;   /* 🔥 Önceki 26–32px seviyesinin TAM 2× */
    line-height: 1.9 !important;
    color: #111 !important;
  }

  /* HEADER — çok daha büyük */
  header, nav, header *, nav * {
    font-size: 48px !important;
    font-weight: 600 !important;
  }

  /* Başlık satırı “Dual AI Console — Zero@Ecosystem” */
  h1, .title, .header-title {
    font-size: 58px !important;
    font-weight: 700 !important;
  }

  /* Model seçiciler (gpt-4.1 mini, Sonnet 4.5, vb.) */
  .model-chip, .model-selector, .chip, .badge, .tag {
    font-size: 40px !important;
    padding: 12px 28px !important;
    font-weight: 600 !important;
  }

  /* Textarea giriş alanı */
  textarea, .prompt-input {
    font-size: 50px !important;    /* önceki boyutun 2×’i */
    line-height: 1.9 !important;
    padding: 22px !important;
    font-family: var(--base-font) !important;
  }

  textarea::placeholder {
    font-size: 46px !important;    /* 🔥 “Cevap burada görünecek…” için 2× */
    color: #777 !important;
  }

  /* Cevap kutuları (ChatGPT & Claude) */
  #openai-output,
  #anthropic-output,
  .response-box,
  .output-box,
  pre, code {
    font-size: 50px !important;   /* 🔥 iki taraf da devasa ve eşit */
    line-height: 1.9 !important;
    white-space: pre-wrap !important;
    font-family: var(--base-font) !important;
  }

  /* Claude tarafının placeholder’ı */
  .claude-placeholder, #claude-output::before {
    font-size: 46px !important;
  }

  /* Butonlar */
  button {
    font-size: 38px !important;
    padding: 18px 30px !important;
    font-weight: 600 !important;
  }
</style>
<!-- /GLOBAL SUPER FONT OVERRIDE -->


<!-- CLAUDE PLACEHOLDER + TOKENS FONT FIX -->
<style>
  /* Claude tarafı çıktı alanı + placeholder */
  #anthropic-output,
  #anthropic-output * {
    font-size: 32px !important;
    line-height: 1.7 !important;
    color: #111 !important;
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", Helvetica, Arial, sans-serif !important;
  }

  /* Ortak placeholder metinleri (her iki taraf için de) */
  .output-placeholder,
  .placeholder-text {
    font-size: 32px !important;
    color: #555 !important;
  }

  /* File upload ve Tokens satırı */
  small,
  .token-info,
  .token-count,
  .tokens-label {
    font-size: 24px !important;
    line-height: 1.5 !important;
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", Helvetica, Arial, sans-serif !important;
  }

  /* Özellikle dosya seçim label’ı */
  label[for*="file"],
  .file-label {
    font-size: 24px !important;
    font-weight: 500 !important;
  }
</style>
<!-- /CLAUDE PLACEHOLDER + TOKENS FONT FIX -->


<!-- ZERO@ECOSYSTEM DUAL CONSOLE GLOBAL THEME v2 -->
<style>
  /* Genel gövde */
  body {
    margin: 0 !important;
    padding: 0 !important;
    background: #ffffff !important;
    color: #111111 !important;
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", Helvetica, Arial, sans-serif !important;
    font-size: 18px !important;
    line-height: 1.6 !important;
  }

  /* Ana layout panelleri */
  #app,
  .container,
  .panel,
  .left-panel,
  .right-panel {
    background: #ffffff !important;
    color: #111111 !important;
  }

  /* Üst başlık */
  header,
  .top-bar,
  .app-header,
  .app-title {
    font-size: 26px !important;
    font-weight: 700 !important;
    letter-spacing: 0.02em;
  }

  /* Alt başlık / açıklama satırı */
  .subtitle,
  .subheading,
  .header-subtitle {
    font-size: 18px !important;
    font-weight: 500 !important;
  }

  /* Model seçici pill’ler (gpt-4.1 / Sonnet vs.) */
  .model-pill,
  .model-tag,
  .model-select,
  .model-label {
    font-size: 18px !important;
    font-weight: 600 !important;
  }

  /* Prompt input alanları */
  textarea,
  .prompt-input,
  #chatgpt-input,
  #anthropic-input {
    font-size: 20px !important;
    line-height: 1.7 !important;
    color: #111111 !important;
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", Helvetica, Arial, sans-serif !important;
  }

  textarea::placeholder,
  .prompt-input::placeholder {
    font-size: 20px !important;
    color: #999999 !important;
  }

  /* Çıktı (cevap) alanları */
  #chatgpt-output,
  #anthropic-output,
  .output-box,
  .output-panel {
    font-size: 22px !important;
    line-height: 1.8 !important;
    color: #111111 !important;
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", Helvetica, Arial, sans-serif !important;
    background: #ffffff !important;
  }

  #chatgpt-output p,
  #chatgpt-output pre,
  #anthropic-output p,
  #anthropic-output pre {
    font-size: 22px !important;
    line-height: 1.8 !important;
  }

  /* Placeholder metinler: “Cevap burada görünecek...” */
  .output-placeholder,
  .placeholder-text {
    font-size: 22px !important;
    color: #666666 !important;
  }

  /* Butonlar (Send to OpenAI / Send to Claude / Choose File etiketleri) */
  button,
  .btn,
  .send-btn,
  .upload-label,
  label[for*="file"],
  .file-label {
    font-size: 18px !important;
    font-weight: 600 !important;
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", Helvetica, Arial, sans-serif !important;
  }

  /* Tokens bilgisi ve ufak yazılar */
  small,
  .token-info,
  .token-count,
  .tokens-label,
  .footer-tip,
  .hint-text {
    font-size: 16px !important;
    line-height: 1.5 !important;
  }

  /* Alt bar (CHATGPT PROJECTS / CLAUDE PROJECTS) */
  footer,
  .footer,
  .bottom-bar {
    font-size: 16px !important;
    font-weight: 500 !important;
  }
</style>


<!-- CLAUDE OUTPUT FONT FIX -->
<style>
  /* Claude tarafındaki tüm olası cevap alanları */
  #anthropic-output,
  .anthropic-output,
  #claude-output,
  .claude-output,
  .output-right,
  .right-output,
  .claude-response,
  #response-claude,
  .output-box-right,
  .output-panel-right,
  .output-text-right {
      font-size: 22px !important;
      line-height: 1.8 !important;
      color: #111111 !important;
      background: #ffffff !important;
      font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                    "Helvetica Neue", Helvetica, Arial, sans-serif !important;
  }

  /* İçindeki p, div, span hepsi büyüsün */
  #anthropic-output * ,
  .anthropic-output * ,
  #claude-output *,
  .claude-output *,
  .output-right * ,
  .right-output * ,
  .claude-response * ,
  .output-box-right *,
  .output-panel-right * {
      font-size: 22px !important;
      line-height: 1.8 !important;
      color: #111111 !important;
  }

  /* Placeholder düzeltmesi */
  #anthropic-output::before,
  .anthropic-output::before {
      font-size: 22px !important;
      color: #777 !important;
  }
</style>


<!-- GLOBAL FONT OVERRIDE v2 -->
<style>
  /* Genel gövde – her şey buradan büyüsün */
  body {
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", Helvetica, Arial, sans-serif !important;
    font-size: 20px !important;   /* temel boyut (önceki ~10–11’in 2x’i) */
    line-height: 1.8 !important;
    color: #111111 !important;
    background: #ffffff !important;
  }

  /* Başlık ve üst bar */
  header, h1, h2, h3, nav, .header-bar, .top-bar {
    font-size: 22px !important;
    font-weight: 600 !important;
  }

  /* Model rozetleri (gpt-4.1 / mini, Sonnet 4.5 vs.) */
  .model-pill, .model-tag, .model-badge, .pill, .chip {
    font-size: 16px !important;
    font-weight: 500 !important;
  }

  /* Prompt input alanları */
  textarea, input[type="text"], .prompt-input, .prompt-area {
    font-size: 18px !important;
    line-height: 1.7 !important;
  }

  /* Ortadaki info / tip yazıları */
  small, .helper-text, .hint, .subtext {
    font-size: 14px !important;
  }

  /* OUTPUT PANELLER – hem ChatGPT hem Claude */
  .output-panel,
  .output-box,
  .output,
  .response-area,
  .response-box,
  #openai-output,
  #chatgpt-output,
  #anthropic-output,
  #claude-output {
    font-size: 20px !important;
    line-height: 1.8 !important;
    color: #111111 !important;
    background: #ffffff !important;
  }

  .output-panel *,
  .output-box *,
  .output *,
  .response-area *,
  .response-box *,
  #openai-output *,
  #chatgpt-output *,
  #anthropic-output *,
  #claude-output * {
    font-size: 20px !important;
    line-height: 1.8 !important;
    color: #111111 !important;
  }

  /* “Cevap burada görünecek...” placeholder’ları */
  .output-placeholder,
  .placeholder,
  #openai-output::before,
  #anthropic-output::before,
  #claude-output::before {
    font-size: 20px !important;
    color: #777777 !important;
  }

  /* Butonlar (Send to OpenAI / Send to Claude / Choose Files vs.) */
  button,
  .btn,
  .btn-primary,
  .btn-secondary {
    font-size: 16px !important;
    font-weight: 600 !important;
  }

</style>


<!-- GLOBAL FONT OVERRIDE v3 – XXL EDITION -->
<style>
  /* Ana gövde – büyük, temiz, Apple tarzı */
  body {
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", Helvetica, Arial, sans-serif !important;
    font-size: 28px !important;      /* Bir öncekinin yaklaşık 2 katı */
    line-height: 1.9 !important;
    color: #111 !important;
    background: #fff !important;
  }

  /* Başlık + Üst bar */
  header, h1, h2, h3, nav, .header-bar, .top-bar {
    font-size: 30px !important;
    font-weight: 700 !important;
  }

  /* Model rozetleri (gpt-4.1 / mini, Sonnet 4.5 vs.) */
  .model-pill, .model-tag, .model-badge, .pill, .chip {
    font-size: 22px !important;
    font-weight: 600 !important;
  }

  /* Prompt input alanları */
  textarea, input[type="text"], .prompt-input, .prompt-area {
    font-size: 26px !important;
    line-height: 1.9 !important;
  }

  /* "Cevap burada görünecek..." */
  .output-placeholder,
  .placeholder,
  #openai-output::before,
  #claude-output::before,
  #anthropic-output::before {
    font-size: 26px !important;
    color: #666 !important;
  }

  /* Output paneller – hem ChatGPT hem Claude */
  .output-panel,
  .output-box,
  .output,
  .response-area,
  .response-box,
  #openai-output,
  #chatgpt-output,
  #anthropic-output,
  #claude-output {
    font-size: 28px !important;
    line-height: 1.9 !important;
    color: #111 !important;
  }

  .output-panel *,
  .output-box *,
  .output *,
  .response-area *,
  .response-box *,
  #openai-output *,
  #claude-output * {
    font-size: 28px !important;
  }

  /* Butonlar */
  button,
  .btn,
  .btn-primary,
  .btn-secondary {
    font-size: 22px !important;
    font-weight: 700 !important;
  }

  /* Choose Files ve Tokens */
  label, .file-label, .token-label, .token-count, span {
    font-size: 22px !important;
  }
</style>


<!-- GLOBAL FONT OVERRIDE v4 – XXL + Layout -->
<style>
  body {
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
                 "Helvetica Neue", Helvetica, Arial, sans-serif !important;
    font-size: 32px !important;      /* temel body */
    line-height: 1.9 !important;
    color: #111 !important;
    background: #fff !important;
  }

  /* Ana başlık ve üst bar */
  header, h1, h2, h3, nav, .header-bar, .top-bar {
    font-size: 34px !important;
    font-weight: 700 !important;
  }

  /* Model rozetleri (gpt-4.1 / mini, Sonnet 4.5…) */
  .model-pill, .model-tag, .model-badge, .pill, .chip {
    font-size: 24px !important;
    font-weight: 600 !important;
  }

  /* Prompt input alanları */
  textarea, input[type="text"], .prompt-input, .prompt-area {
    font-size: 32px !important;
    line-height: 1.9 !important;
  }

  /* “Cevap burada görünecek…” */
  .output-placeholder,
  .placeholder,
  #openai-output::before,
  #claude-output::before,
  #anthropic-output::before {
    font-size: 36px !important;
    color: #666 !important;
  }

  /* Çıktı panelleri (iki taraf da) */
  .output-panel,
  .output-box,
  .output,
  .response-area,
  .response-box,
  #openai-output,
  #chatgpt-output,
  #anthropic-output,
  #claude-output {
    font-size: 40px !important;      /* asıl hedef burası */
    line-height: 1.9 !important;
    color: #111 !important;
  }

  .output-panel *,
  .output-box *,
  .output *,
  .response-area *,
  .response-box *,
  #openai-output *,
  #claude-output * {
    font-size: 40px !important;
  }

  /* Butonlar + Choose Files + Tokens */
  button,
  .btn,
  .btn-primary,
  .btn-secondary {
    font-size: 24px !important;
    font-weight: 700 !important;
  }

  label,
  .file-label,
  .token-label,
  .token-count,
  span.tokens-label {
    font-size: 24px !important;
  }

  /* İki paneli küçült, araya boşluk koy */
  .columns,
  .console-columns,
  .panels,
  .main-panels {
    max-width: 90vw !important;      /* yatayda %10 boşluk */
    margin: 0 auto 24px auto !important;
    display: flex !important;
    gap: 24px !important;
  }

  .column,
  .panel-column,
  .console-column {
    width: 46% !important;           /* her biri ~%25 küçülmüş gibi */
  }

  /* Claude header’ını turuncu yap (Send to OpenAI rengi gibi) */
  #claude-header,
  #claude-panel-header,
  #claude-column .panel-header,
  .claude-header {
    background: #f9ba00 !important;  /* senin turuncu */
    color: #fff !important;
  }

  #claude-header *,
  #claude-panel-header *,
  #claude-column .panel-header *,
  .claude-header * {
    color: #fff !important;
  }
</style>
\n
<style>
  /* CLAUDE HEADER BUTTON BAR — TURUNCU EDITION */
  #claude-header, 
  .claude-header, 
  #claude-column .panel-header,
  .panel-header.claude {
      background: #f9ba00 !important;
      color: #fff !important;
      padding: 16px 24px !important;
      border-radius: 12px !important;
  }

  #claude-header *,
  .claude-header *,
  #claude-column .panel-header *,
  .panel-header.claude * {
      color: #fff !important;
      font-weight: 700 !important;
      font-size: 32px !important;
  }

  /* Model seçici butonları da unify edelim (Sonnet / Opus / Haiku) */
  .model-pill,
  .model-chip,
  .model-tag {
      background: #f9ba00 !important;
      color: #fff !important;
      padding: 12px 24px !important;
      border-radius: 24px !important;
      font-size: 28px !important;
      font-weight: 700 !important;
  }

  /* Hover efekti — Apple vari hafif koyulaşma */
  .model-pill:hover,
  .model-chip:hover,
  .model-tag:hover {
      background: #d79a00 !important;
  }

  /* PANEL DARALTMA + BOŞLUK */
  .console-panels,
  .columns,
  .main-panels {
      display: flex !important;
      justify-content: center !important;
      align-items: flex-start !important;
      gap: 32px !important;       /* iki blok arası boşluk */
      width: 90vw !important;     /* %25 daraltmanın karşılığı */
      margin: 0 auto !important;
  }

  .panel,
  .panel-column {
      width: 45% !important;      /* iki panel %45 + gap */
      height: 75vh !important;    /* %25 boydan küçültme */
  }
</style>


<style>
  /* PANEL SIZE FIX — %90 original */
  .console-panels,
  .columns,
  .main-panels {
      display: flex !important;
      justify-content: center !important;
      align-items: flex-start !important;
      gap: 28px !important;     /* hoş küçük boşluk */
      width: 95vw !important;   /* %90 genişlik için 95vw */
      margin: 0 auto !important;
  }

  .panel,
  .panel-column {
      width: 48% !important;    /* %45 çok küçüktü → %48 eskiye yakın */
      height: 88vh !important;  /* %75 çok küçüktü → %88 ideal */
  }
</style>

</head>
<body>
<header>
  <div>
    <h1>Dual AI Console — Zero@Ecosystem</h1>
    <span>Left: ChatGPT/OpenAI · Right: Claude (Anthropic)</span>
  </div>
  <span>Local · Pay-as-you-go via API keys</span>
</header>
<main>
  <!-- LEFT PANEL: OPENAI / CHATGPT -->
  <section class="panel" id="openai-panel">
    <div class="panel-header">
      <h2>🧡 ChatGPT / OpenAI <span class="badge">gpt-4.1 / mini</span></h2>
      <select id="openai-model">
        <option value="gpt-4.1-mini">gpt-4.1-mini</option>
        <option value="gpt-4.1">gpt-4.1</option>
      </select>
    </div>
    <div class="body">
      <textarea id="openai-input" placeholder="Buraya bana soracağın şeyi yaz... (solda Chatty / OpenAI tarafı)"></textarea>
      <div class="controls">
        <button id="openai-send">Send to OpenAI</button>
        <input type="file" id="openai-file" multiple />
        <span class="usage" id="openai-usage">Tokens: -</span>
      </div>
      <div class="output" id="openai-output">Cevap burada görünecek...</div>
    </div>
  </section>

  <!-- RIGHT PANEL: CLAUDE -->
  <section class="panel" id="claude-panel">
    <div class="panel-header">
      <h2>💙 Claude / Anthropic <span class="badge">Sonnet / Opus / Haiku</span></h2>
      <select id="claude-model">
        <option value="claude-sonnet-4-5-20250929">Sonnet 4.5</option>
        <option value="claude-opus-4-5-20251101">Opus 4.5</option>
        <option value="claude-haiku-4-5-20251001">Haiku 4.5</option>
      </select>
    </div>
    <div class="body">
      <textarea id="claude-input" placeholder="Buraya Claude tarafına soracağın şeyi yaz... (sağda strateji / analiz / kod vs.)"></textarea>
      <div class="controls">
        <button id="claude-send">Send to Claude</button>
        <input type="file" id="claude-file" multiple />
        <span class="usage" id="claude-usage">Tokens: -</span>
      </div>
      <div class="output" id="claude-output">Cevap burada görünecek...</div>
    </div>
  </section>
</main>
<footer>
  <span>Tip: Aynı promptu iki tarafa da gönderip cevapları kıyaslayabilirsin.</span>
  <span>Zero@Ecosystem · Local Dev Console</span>
</footer>

<script>
async function sendChat(side) {
  const inputEl = document.getElementById(side + "-input");
  const outputEl = document.getElementById(side + "-output");
  const usageEl = document.getElementById(side + "-usage");
  const modelEl = document.getElementById(side + "-model");
  const fileEl = document.getElementById(side + "-file");
  const btnEl = document.getElementById(side + "-send");

  const prompt = inputEl.value.trim();
  if (!prompt) return;

  const model = modelEl.value;
  const endpoint = side === "openai" ? "/api/chat/openai" : "/api/chat/claude";

  const formData = new FormData();
  formData.append("prompt", prompt);
  formData.append("model", model);
  formData.append("max_tokens", "600");

  if (fileEl.files && fileEl.files.length > 0) {
    for (const f of fileEl.files) {
      formData.append("files", f);
    }
  }

  btnEl.disabled = true;
  btnEl.textContent = "Working...";
  outputEl.textContent = "Thinking...";

  try {
    const res = await fetch(endpoint, {
      method: "POST",
      body: formData,
    });

    if (!res.ok) {
      const errText = await res.text();
      outputEl.textContent = "Error: " + errText;
      usageEl.textContent = "Tokens: error";
      return;
    }

    const data = await res.json();
    if (data.error) {
      outputEl.textContent = "Error: " + data.error;
      usageEl.textContent = "Tokens: error";
      return;
    }

    outputEl.textContent = data.answer || "(boş cevap)";
    const usage = data.usage || {};
    const inTok = usage.input_tokens ?? usage.prompt_tokens ?? "-";
    const outTok = usage.output_tokens ?? usage.completion_tokens ?? "-";
    usageEl.textContent = `Tokens → in: ${inTok} · out: ${outTok}`;
  } catch (e) {
    outputEl.textContent = "Error: " + e;
    usageEl.textContent = "Tokens: error";
  } finally {
    btnEl.disabled = false;
    btnEl.textContent = side === "openai" ? "Send to OpenAI" : "Send to Claude";
  }
}

document.getElementById("openai-send").addEventListener("click", () => sendChat("openai"));
document.getElementById("claude-send").addEventListener("click", () => sendChat("claude"));

document.getElementById("openai-input").addEventListener("keydown", (e) => {
  if (e.key === "Enter" && (e.metaKey || e.ctrlKey)) {
    e.preventDefault();
    sendChat("openai");
  }
});
document.getElementById("claude-input").addEventListener("keydown", (e) => {
  if (e.key === "Enter" && (e.metaKey || e.ctrlKey)) {
    e.preventDefault();
    sendChat("claude");
  }
});
</script>

<!-- PROJECT_HISTORY_START -->
<style>
  #project-history {
    display: flex;
    gap: 24px;
    padding: 10px 16px;
    border-bottom: 1px solid #222;
    background: #05070b;
    font-family: system-ui, -apple-system, BlinkMacSystemFont, "SF Pro Text", sans-serif;
    position: sticky;
    top: 0;
    z-index: 50;
  }
  .history-column {
    flex: 1;
  }
  .history-column h3 {
    font-size: 13px;
    margin: 0 0 6px 0;
    color: #aaa;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
  .history-column ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }
  .history-column li {
    display: flex;
    align-items: center;
  }
  .history-pill {
    font-size: 12px;
    padding: 4px 10px;
    border-radius: 999px;
    border: 1px solid #333;
    background: #111;
    color: #eee;
    cursor: pointer;
    white-space: nowrap;
  }
  .history-pill:hover {
    border-color: #f9ba00;
  }
  .history-link {
    margin-left: 4px;
    font-size: 11px;
    color: #888;
    text-decoration: none;
  }
  .history-link:hover {
    color: #f9ba00;
  }
  @media (max-width: 900px) {
    #project-history {
      flex-direction: column;
      gap: 12px;
    }
  }
</style>

<div id="project-history">
  <div class="history-column">
    <h3>ChatGPT Projects</h3>
    <ul id="openai-history-list"></ul>
  </div>
  <div class="history-column">
    <h3>Claude Projects</h3>
    <ul id="claude-history-list"></ul>
  </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', async () => {
  try {
    const res = await fetch('/static/history.json');
    if (!res.ok) throw new Error('HTTP ' + res.status);
    const data = await res.json();

    const openaiList = document.getElementById('openai-history-list');
    const claudeList = document.getElementById('claude-history-list');

    function addItem(item, parent) {
      const li = document.createElement('li');

      const btn = document.createElement('button');
      btn.className = 'history-pill';
      btn.textContent = item.label;
      btn.onclick = () => {
        if (item.url) {
          window.open(item.url, '_blank', 'noopener,noreferrer');
        }
      };

      li.appendChild(btn);

      if (item.url) {
        const a = document.createElement('a');
        a.className = 'history-link';
        a.href = item.url;
        a.target = '_blank';
        a.rel = 'noopener noreferrer';
        a.textContent = '↗';
        li.appendChild(a);
      }

      parent.appendChild(li);
    }

    (data.openai || []).forEach(it => addItem(it, openaiList));
    (data.claude || []).forEach(it => addItem(it, claudeList));
  } catch (err) {
    console.error('Project history load error:', err);
  }
});
</script>
<!-- PROJECT_HISTORY_END -->

</body>
</html>
